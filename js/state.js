/**
 * state.js
 * 
 * Client-side global state management (Store). Observes variables like
 * UI active states, loaded clubs, or authentication state and publishes events.
 */
export const state = {
    clubs: [],
    executive: [],
    theme: 'light'
};

